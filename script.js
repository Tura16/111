const person = ({firstName, lastName, age}) => {
	let obj = {
		firstName: firstName,
		lastName: lastName,
		age: age,
		getFullName () {
			return `${firstName} ${lastName}`
		}
	}
	return 
}

console.log("Артур", "Мнацаканян", 25);
